# Custom layouts

Add an `.astro` or `.vue` component here and use its filename as a page's `pageLayout`. The component renders inside the Plume navigation/footer shell and receives `content`, `entry`, `lang`, and `route` props.
