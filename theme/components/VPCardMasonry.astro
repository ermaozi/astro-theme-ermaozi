---
const { cols = { sm: 2, md: 2, lg: 3 }, gap = 16, class: className = '', ...attrs } = Astro.props
---

<div {...attrs} class:list={['vp-card-masonry', className]} data-masonry-cols={JSON.stringify(cols)} data-masonry-gap={String(gap)}>
  <slot />
</div>
