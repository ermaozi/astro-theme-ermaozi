---
import { userSlotComponent } from '../lib/user-slots'

const { name, ...props } = Astro.props as { name: string, [key: string]: unknown }
const Component = userSlotComponent(name)
---

{Component && <Component {...props} />}
