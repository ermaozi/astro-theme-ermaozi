// @ts-check

/**
 * Adds editor completion and type checking without changing the configuration.
 * @template {import('./config-types.ts').SiteConfig} T
 * @param {T} config
 * @returns {T}
 */
export const defineSiteConfig = config => config
